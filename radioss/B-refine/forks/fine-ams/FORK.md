# B-refine fine — Kareem /AMS (not the grade row)

Vanilla STOP 1e-6 CFL'd at t≈1.3 ms before λ≥2. `/AMS` + `/DT/AMS` Tmin=5e-6 (same μ/ρ/Ishell=1) reached 20/22 ms then dt collapsed at t≈27 ms (hang, not shell rupture). Hang guard remains STOP. Working nested tape is `../fine-stop5e7`.
