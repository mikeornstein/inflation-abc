# OpenRadioss — letter A inflate first light

SI deck for Inflation ABC letter **A** (`meshes/A.json`, N=1554, 1540 quads, 28 orphan tris).
Physics first: **same μ**, **ρ = 1130 kg/m³**, kiss gap tied to H0. Not MONVOL.

## Units (SI)

| Quantity | Unit |
|----------|------|
| mass | kg |
| length | m |
| time | s |
| pressure / μ | Pa |
| density | kg/m³ |

`/BEGIN` input = work = `kg`, `m`, `s`.

## Locked constitutive labels (do not retune)

| Symbol | Value | Source |
|--------|------:|--------|
| μ₁ | `(800 * 6894.757) / 1.75` ≈ 3.151888914e6 Pa | grill eng. — not invented |
| α₁ | 2 | LAW42 neo-Hookean 1-term |
| μₚ, αₚ (p≥2) | 0 | |
| ν | 0.495 | ≤ 0.495 |
| Prony M | 0 | no viscosity fit |
| H0 | `0.015 * 0.0254` = 3.81e-4 m | 0.015 in film |
| ρ | **1130 kg/m³** | **Desmopan 85085A ISO 1183-1**. McMaster **1446T11 density not published** — this is the desked value, not a guess from the part number. |
| Gapmin | CONTACT_KISS = max(2·H0, 1e-4) = 7.62e-4 m | do not soften to look puffy |
| WARN_LAM | 2.0 | first λ_max ≥ 2 |

LAW42 Ogden SED with μ₁, α₁=2 is neo-Hookean: \(W = (\mu_1/2)(\bar\lambda_1^2+\bar\lambda_2^2+\bar\lambda_3^2-3)\). Initial shear \(\mu = \mu_1\alpha_1/2 = \mu_1 =\) MU.

## Deck

Generated by `python3 tools/mesh_to_radioss.py` (do not hand-edit μ/ρ):

- `Ainflate_0000.rad` — starter (`/NODE`, `/SHELL`, `/SH3N` orphans, `/PART`, `/PROP/SHELL` N=1 Ismstr=10, `/MAT/LAW42`, `/INTER/TYPE19`, `/PLOAD`+`/FUNCT`)
- `Ainflate_0001.rad` — engine (`/ANIM/DT`, strain/thickness/contact, `/DYREL`)
- `law-card.txt` — dump of the law + contact + gap

Try-first:

- Shell PROP **N=1**, **I_smstr=10**, QEPH (`Ishell=24`), `Ithick=1`
- Self-contact **TYPE19** (TYPE7+TYPE11), `Igap=1000` so gap is **Gapmin**, `Irem_gap=2`
- Pressure **`/PLOAD` ramp** 0 → 65 kPa (load path only; μ unchanged). JS Chiron warn was ~54.1 kPa.

## Run (cloud)

```bash
bash radioss/install_openradioss.sh
python3 tools/mesh_to_radioss.py --check
bash radioss/A-inflate/run.sh
```

Install pulls GitHub release `OpenRadioss_linux64.zip` (gfortran linux64: `starter_linux64_gf`, `engine_linux64_gf`, `anim_to_vtk_linux64_gf`) into `radioss/opt/` (gitignored). See `RUN.md` for the job that actually ran.

## Post

`tools/radioss_post.py` converts ANIM → VTK, reports λ_max(t), V(t), p(t), Ψ(t)≥0, contact min gap / punch-through, writes GIF/MP4 with the first λ≥2 frame labeled.

## Out of scope

- Web-mbd JS neo-Hookean path (`index.html`) — do not touch
- MONVOL airbag (later light, not this one)
- Retuning μ or ρ to match the JS volume
